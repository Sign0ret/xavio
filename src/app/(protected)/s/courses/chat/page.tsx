import { Metadata } from 'next';
import React from 'react';

export const generateMetadata = (): Metadata => {
    return {
      title: `Chat`,
      description: `Chats of the different subscribed classes`
    }
  } 
  
export default function Chat() {
    return (
        <div>
        </div>
    )
}